services:
  - type: web
    name: amaran-backend
    runtime: node
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PYTHON_PATH
        value: python3
      - key: PORT
        value: 3001
